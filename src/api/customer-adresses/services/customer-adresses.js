'use strict';

/**
 * customer-adresses service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::customer-adresses.customer-adresses');
