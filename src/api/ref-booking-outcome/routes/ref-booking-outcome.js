'use strict';

/**
 * ref-booking-outcome router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::ref-booking-outcome.ref-booking-outcome');
